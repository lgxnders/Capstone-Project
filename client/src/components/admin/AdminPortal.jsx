// this is the page where the resources can be managed by the admin users, utilizing CRUD operations and making API calls
//Just the setup for future resource management
const express = express();
const router = express.Router();

const getAllResources = async () => {
  // function to fetch all resources from the backend API
};

const addResource = async () => {
  // function to add a new resource via the backend API
};

const updateResource = async () => {
  // function to update an existing resource via the backend API
};

const deleteResource = async () => {
  // function to delete a resource via the backend API
};

export {
  getAllResources,
  addResource,
  updateResource,
  deleteResource,
  express,
  router,
};